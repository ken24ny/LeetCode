class Solution:
    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:
        if len(nums1) > len(nums2):
            # do ths
        else:
            # do something ele

    def compare(self, nums1: List[int], nums2: List[int]) -> List[int]:
        result = []
        for i in nums1:
            if 
